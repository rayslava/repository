# Available init script modifiers :
# - user : the user used to run your mongodb instance (default : rdm)
# - group : the group used to run your mongodb instance (default : rdm)
# - run_dir : the run directory for your PID files (default : /run/)
user="v.barinov"
group="v.barinov"
logfile="/var/log/rdm.log"
ionice=3
nicelevel=15
